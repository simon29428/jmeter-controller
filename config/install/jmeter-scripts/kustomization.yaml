apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: jmeter-system
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
- name: jmeter-scripts
  files:
    - master.sh
    - slave.sh